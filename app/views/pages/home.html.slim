.l_event_banner
  .banner_info
    .star
      h3 Маркет
    h2 Ближайший маркет 26 января арт-пространстве №2
  = image_tag(Item.all.sample.images[0])

div
  .spacing-xl-h
h1 Новая коллекция
div
  .spacing-m-h

.l_item_cards
  = render partial: 'items/item_card', collection: Item.all.limit(4), as: :item

div
  .spacing-xl-h
h1 Новая коллекция
div
  .spacing-m-h

.look_cards
  = render 'looks/look_card_l', look: Look.first
  = render 'looks/look_card_r', look: Look.last

div
  .spacing-xl-h
h1 Золотая классика
div
  .spacing-m-h

.s_item_cards
  = render partial: 'items/item_card', collection: Item.all, as: :item

div
  .spacing-huge-h
